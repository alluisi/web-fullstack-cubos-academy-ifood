import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    root: {
        width: 350,
        height: 'auto',
        marginBottom: 20
    },
    media: {
        height: 'auto',
        width: '100%'
    },
    cardHeader: {
        textAlign: 'center'
    }
}));